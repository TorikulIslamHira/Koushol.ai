# features/chapters/

Reading a chapter and tracking per-chapter progress: `ChapterReader` (content display), `ChapterSidebar` (lock-aware nav), and `useChapterProgress` (a student's quiz_score/completed_at for one chapter).

Chapter *content* comes from `features/courses/useCourse`; this folder is specifically the reading/progress experience once you're on a chapter.
